{
  "name": "veltrawave-vtu",
  "version": "1.0.0",
  "description": "VeltraWave VTU Dashboard",
  "main": "index.js",
  "scripts": {
    "build": "node build.js && cp -r *.html *.js *.json public/ 2>/dev/null || true",
    "postbuild": "echo 'Build completed successfully!'"
  },
  "dependencies": {
    "firebase": "^9.22.0"
  }
}

